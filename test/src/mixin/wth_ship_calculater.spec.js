var withShipCalculator = require('../../../src/js/mixin/with_ship_calculator');

describe('mixin', function(){
  describe('mixin/with_ship_calculator', function(){

    beforeEach(function() {

    });

    it('should be defined', function() {
      expect(withShipCalculator).to.be.defined;
    });

  });
});
