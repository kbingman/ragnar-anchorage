var withUtils = require('ship/with_utils');

describe('ship/with_utils', function(){

  beforeEach(function() {

  });

  it('should be defined', function() {
    expect(withUtils).to.be.defined;
  });

});
